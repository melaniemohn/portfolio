
// add images to each of these
// also, certain projects, like align and times two, will have "watch it in action"... link to youtube?

/* BASIC FORMAT
const PROJECT = {
	name: xxx,
	image: xxx,
	tagline: xxx,
	description: xxx,
	deployedUrl: xxx,
	github: xxx
}
*/

const align = {
	name: 'Align',
	deployedUrl: 'align.fun'
};

const timestwo = {
	name: 'Times Two'
};

const techtalk = {
	name: 'Tech Talk'
};

const graceshopper = {
	name: 'Bits & Bytes'
};

// add contact info for icons, too?

const projects = [align, timestwo, techtalk, graceshopper];

module.exports = {projects};
